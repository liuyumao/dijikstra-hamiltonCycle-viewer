/* Author : Liuyumao
 * Time   : 2022.6.xx
 * College: AHU
 * RADIUS : 画六边形矩形点的半径
 * VEXNUM : 哈密顿回路最大支持的顶点数
 */
#ifndef BASE_H
#define BASE_H

#define Min(a,b) ((a)<(b)? (a) :(b))
#define Max(a,b) ((a)>(b)? (a) :(b))
#define Abs(x) ((x) > 0? (x) :(-(x)))
#define RADIUS 18
#define VEXNUM 250
#pragma execution_character_set("utf-8")

#endif // BASE_H
